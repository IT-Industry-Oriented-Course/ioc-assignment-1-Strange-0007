# LLM provider: gemini
LLM_PROVIDER=gemini

# Gemini API (Google AI Studio)
GEMINI_API_KEY=your_gemini_key_here
# Use a value from `python -m scripts.gemini_list_models` (usually starts with `models/`)
GEMINI_MODEL=models/gemini-2.5-flash

# Agent runtime
AUDIT_LOG_PATH=audit_logs\audit.jsonl
SANDBOX_DATA_DIR=clinical_agent\sandbox\data

